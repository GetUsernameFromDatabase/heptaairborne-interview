version: '3'

volumes:
  db-data:

services:
  db:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    env_file: db.env
    volumes:
      - db-data:/var/lib/postgresql/data

  frontend:
    image: 'node:18'
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
      - '24678:24678'
    environment:
      - VITE_API_HOST=localhost # client perspective -- World Wide Web
      - VITE_API_PORT=8000 # client perspective -- World Wide Web

  backend:
    image: 'eclipse-temurin:17'
    depends_on:
      - db
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - '8000:8080'
    env_file: db.env
    environment:
      - DATABASE_HOST=db # inside docker
      - DATABASE_PORT=5432 # inside docker
